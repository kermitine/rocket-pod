# Repository maintenance

## Version reminder

Every repository change must increment `ROCKET_POD_VERSION` in
`chipper/webroot/js/main.js`. Use whole-number versions in order: `V1`, `V2`,
`V3`, and so on. This value is displayed in the Rocket Pod web UI.
